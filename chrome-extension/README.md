# Chrome Extension

Browser extension for screen recording and capture.

## Features
- One-click screen/webcam/microphone recording
- Direct upload to platform
- Local storage before upload
- Recording controls overlay

## Development

```bash
npm install
npm run build
```

Load the `dist/` folder as an unpacked extension in Chrome.
