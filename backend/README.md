# Backend API Server

Node.js + Express API server for the platform.

## Structure
- `routes/` - API endpoints
- `services/` - Business logic
- `workers/` - Background job processors
- `models/` - Database models

## Development

```bash
npm install
npm run dev
```

API runs on http://localhost:3000
